import { GraphEdge } from "./graph-edge";
import { GraphNode } from "./graph-node";

export class DiGraph {
    private _aList: Map<GraphNode, GraphEdge[]>;

    public addNode(node: GraphNode): Boolean {
        if (this._aList.has) {
            return
        }
    }

}
